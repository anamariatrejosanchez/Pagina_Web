#include <iostream>
using namespace std;

//Hacer un arreglo con 7 elementos(pedir datos al usuario )Que encuentre el mayor y menor de los elementos

int main()

{
int arreglo [7];

    cout<<"A continuacion ingresaras 7 datos "<<endl;

for(int i=0; i<7 ;i++)
  { 
      
            cout<<"Ingresa un valor "<<endl;
      cin>>arreglo[i];
  }
  int numMayor=arreglo[0];
  int numMenor=arreglo[0];

 for (int i = 1; i<7;++i) {
        if (arreglo[i] >numMayor)
        {
            numMayor = arreglo[i];
        }
        if (arreglo[i] < numMenor) {
            numMenor = arreglo[i];
        }
    }

    cout<<"El numero mayor es "<<numMayor<<endl;
    cout<<"El numero menor es "<<numMenor<<endl;

    return 0;
}